$(document).ready(function () {
  $(".home-slider").slick({
    dots: true,
    arrows: true,
    autoplay: true,
    autoplaySpeed: 5000,
     prevArrow:$('.prev'),
      nextArrow:$('.next'),

  });
});
